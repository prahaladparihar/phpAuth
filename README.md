# phpAuth
Simple php authentication with session handling.

# Installation
clone the repo https://github.com/prahaladparihar/phpAuth.git <br/>
update the file include/db_config.php to your database credentials <br/>
run the command ``` php -S localhost:port ```

